'use strict';

// When we have callbacks inside callbacks inside callbacks, then it is called callbackHell, in other words, callback hell is a lot of nested callbacks.

// In order to escape callback hell, we use promises