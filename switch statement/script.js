const player = 'Diego Costa'

switch (player) {
    case 'David Villa': // if (player === 'David Villa')

        console.log("A great player who won both Club and International Cups")

        break;
    case 'Diego Costa':// else if (player === 'Diego Costa')

        console.log("An aggressive player who tackles like hell and shoots like a devil.")

        break;
    default: // else {}
        break;
}

// switch always does a strict comparison