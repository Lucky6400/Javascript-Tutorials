'Lucky' // this is a value, and this is true for numbers and booleans also.

let lucky = 'Lucky 👍'; // here 'name' is a variable.
console.log(lucky)

// variables decclared with "var" will create a property on a global "window" object. "window" is a global object of javascript which can be accessed anytime in the browser

// variables declared with "let" and "const" do not create a property