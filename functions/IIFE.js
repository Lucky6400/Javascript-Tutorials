'use strict';

// IMMEDIATELY INVOKED FUNCTION EXPRESSIONS

(() => {
    console.log(`Diego Costa is the best striker!`)
})();

(function(){ 
    console.log(`Chelsea is the best football club ever!`);
})();

// These will never run again.